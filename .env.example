# Railway Environment Variables
# Set these in Railway Dashboard → Variables

# API Keys (required for full pipeline)
LIGHTCAST_API_KEY=your_l...n

# Pipeline control
FORCE_REFRESH=0  # Set to 1 to force re-run pipeline on deploy

# Streamlit
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_ADDRESS=0.0.0.0

# Optional: Analytics
SENTRY_DSN=
PLAUSIBLE_DOMAIN=

# --- LLM Intelligence Layer ---
# Provider-agnostic via LiteLLM. Default is Anthropic Claude.
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Per-tier model overrides (optional; sensible Claude defaults apply)
LLM_MODEL_BATCH=
LLM_MODEL_INTERACTIVE=
LLM_MODEL_HARD=
# Hard ceiling on output tokens per run (safety guard)
LLM_TOKEN_BUDGET=2000000

# Public-deploy cost guards for the Ask page
ASK_SESSION_LIMIT=10           # questions per browser session
ASK_DAILY_TOKEN_CAP=200000     # shared daily token ceiling

# Skill source for the dashboard: 'llm' (default; falls back to dictionary if
# job_skills_llm is absent) or 'flashtext'
SKILLS_METHOD=llm

# --- Lead capture (in-app CTAs) ---
# Set any of these to activate the "Stay in the loop" CTAs. If only CONTACT_EMAIL
# is set, the buttons become pre-filled mailto links (works with zero external tools).
BRIEF_SIGNUP_URL=      # newsletter/form for the monthly Market Brief (Beehiiv/Substack/Tally/Google Form)
WAITLIST_URL=          # beta waitlist form
PILOT_BOOKING_URL=     # booking/form for teams, coaches, schools (Cal.com/Calendly/Tally)
CONTACT_EMAIL=         # universal mailto fallback for all three CTAs
